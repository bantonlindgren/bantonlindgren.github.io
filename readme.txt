$npm install -g gulp-cli gulp
$npm install -g @angular/cli


Run app:
 $ ng serve --open


Create new component
 $ ng generate component <component_name>
 * Add new components to src/app/app.router.ts


FTP
 * rhn
 * Behöver inte ange port

Deploy
 * Run ng build
   * This will create/update /dist folder
 * copy /dist content to /www